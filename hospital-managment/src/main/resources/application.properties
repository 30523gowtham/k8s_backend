# 🌐 Application Identity
spring.application.name=hospital-management

# 🛠️ Server Configuration
server.port=8086
server.servlet.context-path=/back1


# 🗄️ Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hospitalu?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
&allowPublicKeyRetrieval=true
&allowPublicKeyRetrieval=true

spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 🧠 JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 🔐 JWT Configuration
jwt.secret=Gowtham@Hospital2025!SecureKey123456789
# 24 hours in milliseconds
jwt.expiration=86400000

# 🌍 CORS (Optional if configured in code)
cors.allowed-origins=http://localhost:3000

# 📦 Logging (Optional)
logging.level.org.springframework=INFO
logging.level.com.hospital=DEBUG
